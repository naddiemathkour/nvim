# Neovim Config

## Steps to install:

1. Clone branch into ~/.config folder
2. cd into `nvim`
3. Open neovim and install all dependencies
4. Run `sudo apt install xclip` to ensure nvim clipboard matches sys clipboard
